<h1><%= @article.title %></h1>

<p><%= @article.body %></p>

<ul>
  <li><%= link_to "Edit", edit_article_path(@article) %></li>
  <li><%= link_to "Destroy", article_path(@article), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure? This action cannot be undone. The post <%= article.title %> will be deleted forever and BlogCog will not ever allow you to restore it."
                  } %></li>
</ul>
